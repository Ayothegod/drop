{
  "name": "droplane",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter client run dev",
    "dev:backend": "pnpm --filter server run dev",
    "dev": "concurrently \"pnpm --filter client run dev\" \"pnpm --filter server run dev\"",
    "dev:single": "pnpm -r run dev",
    "build:frontend": "pnpm --filter client run build",
    "build:backend": "pnpm --filter server run build",
    "build": "pnpm --filter server run build",
    "real:build": "pnpm -r run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
